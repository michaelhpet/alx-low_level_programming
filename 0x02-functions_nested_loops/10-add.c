#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: first integer (operand)
 * @b: second integer (operand)
 *
 * Return: returns a + b
 */
int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}

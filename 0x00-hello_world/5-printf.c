#include <stdio.h>

/**
 * main - Entry function
 *
 * prints a string
 *
 * Return: 0 (always)
 *
*/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
